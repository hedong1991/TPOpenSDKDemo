<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:background="@color/dialog_loading_background"
              android:minWidth="88dp"
              android:orientation="vertical"
              android:paddingBottom="8dp"
              android:paddingTop="14dp">

    <com.tplink.sdk.tpopensdkdemo.common.RoundProgressBar
        android:id="@+id/dialog_loading_progress_bar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:layout_marginBottom="8dp"
        app:centerColor="@color/transparent"
        app:isTextDisplay="false"
        app:progressColor="@color/white"
        app:radius="18dp"
        app:ringColor="@color/transparent"
        app:ringWidth="2dp"
        app:startAngle="-90"/>

    <TextView
        android:id="@+id/dialog_loading_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:maxWidth="240dp"
        android:paddingLeft="24dp"
        android:paddingRight="24dp"
        android:textColor="@color/white"
        android:textSize="11dp"
        android:visibility="gone"/>

</LinearLayout>